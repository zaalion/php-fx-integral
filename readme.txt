This PHP class calculates the integral value or the passed function f(x) where x is 
between a and b. Tthis class also draws the image diagram. which can be directly downloaded from the browser. Run index.html with initiated values.


Note: GD/GD2 must be installed on your server.